controller Semantics_Difference where

-- Based on an example in slugs (https://github.com/VerifiableRobotics/slugs)

input person (P) : Bool

output bit0 (B) : Bool

sys_trans
  bit0

env_liveness
  person
  !person
